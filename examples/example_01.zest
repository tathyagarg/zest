scene {
    object {
        .type = SPHERE,
        .position = (1, 1, 1),
        .radius = 1,
        .physics = physics_1,
        .material = mat_1,
    }

    camera {
        .position = (0, 0, 0),
        .direction = (0, 0, 0),
        .controller = camera_controller,
    }

    physics_1 {
        .gravity = true
    }

    mat_1 {
        .color = "#d00d1e"
    }

    camera_controller {
        .mouse = true,
        .keyboard = true,
    }
}
