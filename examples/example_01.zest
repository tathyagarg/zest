SCENE scene {
    MATERIAL mat_1 {
        .color = "#d00d1e"
    }

    CONTROLLER camera_controller {
        .mouse = true,
        .keyboard = true,
    }

    OBJECT object {
        .type = SPHERE,
        .position = (1, 1, 1),
        .radius = 1,
        .material = mat_1,
    }

    CAMERA camera {
        .position = (0, 0, 0),
        .direction = (0, 0, 0),
        .controller = camera_controller,
    }

    PHYSICS physics_1 {
        .gravity = true,
        .on = object
    }
}
